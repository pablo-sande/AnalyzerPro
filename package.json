{
  "name": "code-analyzer-pro",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @code-analyzer-pro/web dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clear-cache": "curl -X POST http://localhost:3000/cache/clear"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
